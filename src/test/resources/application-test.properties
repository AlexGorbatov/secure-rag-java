# Tests never call a paid model: embeddings come from the local ONNX model (deterministic),
# and no chat model is created.
spring.ai.model.chat=none
spring.ai.model.embedding=transformers
spring.ai.openai.api-key=not-used-in-tests
